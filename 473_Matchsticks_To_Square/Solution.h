#include <vector>

class Solution {
    bool getStick(std::vector<int>& matchSticks, size_t index, unsigned targetSum, std::vector<unsigned>& currentSum);
public:
    bool makesquare(std::vector<int>& matchsticks);
};
